# Copy this file to .env. The .env file is intentionally ignored by Git.
RUNNER_DOWNLOAD_DIR=/absolute/path/to/downloads
# Reuse one visible background window. Set true only if minimized startup works well on your desktop.
RUNNER_START_MINIMIZED=false
