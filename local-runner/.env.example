# Copy this file to .env. The .env file is intentionally ignored by Git.
RUNNER_DOWNLOAD_DIR=/absolute/path/to/downloads
# Run without a desktop window so background downloads cannot steal focus.
RUNNER_HEADLESS=true
# Keep Chromium from taking focus while queued links are processed.
RUNNER_START_MINIMIZED=true
